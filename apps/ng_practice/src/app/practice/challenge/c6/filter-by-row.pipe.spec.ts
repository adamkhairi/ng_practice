import { FilterByRowPipe } from './filter-by-row.pipe';

describe('FilterByRowPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRowPipe();
    expect(pipe).toBeTruthy();
  });
});
